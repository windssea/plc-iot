# 闭环联调测试报告

日期：2026-09-18  
对象：PLCnext-iot Agent 0.2.0  
Broker：测试脚本在 `127.0.0.1:1883` 拉起的匿名明文 Mosquitto（本机原 1883 已停）  
方案：[闭环联调测试方案](闭环联调测试方案.md)  
原始结果：`.local/closed-loop/results.json`

## 1. 结论

**30 / 30 通过。** 含 T23：停止再启动 Broker 后，Agent 重新 online 并继续上报。

## 2. 环境

| 项 | 实际 |
|---|---|
| MQTT | 脚本启动 `plcnext-iot-closed-loop-mqtt`，映射 `127.0.0.1:1883`，匿名、无 TLS |
| 模拟器 | `D:\work\code\PLCnext-mock-device` |
| Modbus | `127.0.0.1:11502` |
| OPC UA | `opc.tcp://127.0.0.1:14840/iot-simulator/` |
| gatewayId | PLC-01 |

结束后已删除测试用 Mosquitto 容器。

## 3. 用例结果

| 编号 | 场景 | 结果 |
|---|---|---|
| T01–T22 | 环境、下发、采集、改配置、协议掉线、版本规则、隔离、change | **PASS** |
| T23 | Broker 停止 4 秒再启动，恢复后电压继续 GOOD | **PASS** |
| T24 | 停 ACK 期间仍收到 data，恢复后补发 STORED | **PASS** |
| T25 / T30 | Agent 重启 + retained 恢复 | **PASS** |
| T26–T29 | 单台离线、错误 NodeId、缺 OPC UA 密码、混合周期 | **PASS** |
| T17 | 清空子设备 | **PASS** |

## 4. 说明

- 本机 1883 空闲时由脚本自管 Broker，因此可以做 T23。若 1883 又被其它进程占用，T23 会 SKIPPED。
- 关掉 OPC UA 适配器时 asyncua 仍会打 `Connection is closed`，不影响判定。

## 5. 复现

```powershell
./.venv/Scripts/python.exe -m tools.closed_loop_test --mock-project D:/work/code/PLCnext-mock-device --mock-python .local/mock-venv/Scripts/python.exe
```

需要 Docker，以及空闲的 1883、18080、11502、14840。
